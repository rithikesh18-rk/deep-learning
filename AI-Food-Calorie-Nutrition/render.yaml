services:
  - type: web
    name: ai-food-calorie-nutrition
    env: python
    rootDir: AI-Food-Calorie-Nutrition
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
